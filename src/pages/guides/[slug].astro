---
import { guideArticles } from '../../data/guides'

export function getStaticPaths() {
  return Object.keys(guideArticles).map((slug) => ({
    params: { slug },
  }))
}

const { slug } = Astro.params
return Astro.redirect(`/resources/homeowner-guides/${slug}`, 301)
---
